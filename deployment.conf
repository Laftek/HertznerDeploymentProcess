# Only contains EXACT placeholders from the files
APP_NAME="TradingBot"
DOMAIN="bot.yourdomain.com"  # From nginx/conf.d/app.conf
DB_NAME="tradingbot_prod"     # From docker-compose.yml (yourdbname)
DB_USER="tradingbot_user"     # From docker-compose.yml (youruser)
RCLONE_REMOTE="trading_backups" # From backup.sh (backups)